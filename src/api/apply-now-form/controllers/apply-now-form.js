'use strict';

/**
 * apply-now-form controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::apply-now-form.apply-now-form');
